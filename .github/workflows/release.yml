name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore WindroseEditor.sln -r win-x64 --disable-parallel --no-cache

      - name: Publish application
        run: |
          dotnet publish WindroseEditor.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -p:DebugType=none `
            -p:DebugSymbols=false `
            --no-restore `
            -o ./publish

      - name: Create ZIP archive
        run: Compress-Archive -Path ./publish/* -DestinationPath ./WindroseEditor-Release.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: ./WindroseEditor-Release.zip
          generate_release_notes: true